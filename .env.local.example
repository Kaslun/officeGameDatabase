# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your-publishable-key
# Required for admin API (update/delete requests, save config). Never expose to client.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin (server-only)
ADMIN_PASSWORD=your-admin-password
# Optional: unique salt for admin session cookie (defaults to built-in). Set in production.
# ADMIN_SESSION_SALT=your-random-salt

# RAWG Video Games Database API (https://rawg.io/apidocs)
NEXT_PUBLIC_RAWG_API_KEY=your-rawg-api-key

# Optional: editor key to show status-edit UI on /requests (actual save still requires admin login)
# NEXT_PUBLIC_EDITOR_KEY=your-secret-key
